name: diceroller
description: "A simple dice roller."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.0-326.0.dev

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
